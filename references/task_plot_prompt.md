Use case: infographic-diagram
Asset type: TaskBeacon task flow diagram
Primary request: Create a clean, publication-ready task flow diagram as a grouped protocol timeline for the behavioral task described below.

Task: Trier Social Stress Test
Construct: psychosocial stress induction / social-evaluative threat
Rows/segments:
- Baseline and preparation: instruction, baseline acclimation, and speech preparation.
- Speech stress: public speech with judges and recording indicator.
- Arithmetic and recovery: serial subtraction with judges, then quiet recovery and goodbye.

Timeline phases:
- Baseline and preparation: Instruction (100 ms; press SPACE to start) -> Baseline acclimation (300000 ms; no response; quiet fixation) -> Speech preparation (600000 ms; no response; prepare job-fit speech)
- Speech stress: Speech delivery (300000 ms; no response; neutral judges and REC cue visible)
- Arithmetic and recovery: Mental arithmetic (300000 ms; no response; count backward 2043 by 17; judges and REC cue visible) -> Recovery (900000 ms; no response; quiet fixation) -> Goodbye (100 ms; press SPACE to exit)

Visual requirements:
- White background, landscape orientation, crisp dark text, restrained condition accent colors.
- Use three horizontal protocol segments rather than invented trial conditions.
- Each segment contains participant-screen snapshots connected by a subtle arrow.
- Each screen snapshot shows participant-visible screen content only.
- For the evaluative phases, use simple neutral judge silhouettes and a small REC indicator, not detailed people.
- Use gray participant-screen boxes, thin black arrows, consistent row spacing, and subtle row separators.
- Place timing labels under each screen in compact text.
- Place segment labels at the left of each row.
- Use short labels only; avoid paragraphs inside the image.
- Make all text legible at normal document preview size.
- Leave a clean blank header band across the top 18-20% of the image.

Accuracy constraints:
- Do not invent phases, stimuli, condition names, keys, rewards, scoring, or timings.
- Do not add feedback, accuracy, points, tokens, or adaptive control.
- Do not draw the task title, construct subtitle, any logo, watermark, brand mark, or `TaskBeacon` text inside the generated image.
- Draw only the timeline content below the blank header band.
- Preserve these exact terms where used: Instruction, Baseline acclimation, Speech preparation, Speech delivery, Mental arithmetic, Recovery, Goodbye, SPACE, 2043 by 17, REC, 100 ms, 300000 ms, 600000 ms, 900000 ms.

Style:
TaskBeacon scientific infographic style: clean vector-like raster image, organized spacing, gray screen boxes, restrained color accents, and a blank header-safe area.
